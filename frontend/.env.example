# `npm run dev` 전용 · Vite 만 읽음(프록시 대상). 브라우저는 포트 불필요.
# `Start-InventionWriter.cmd` · `scripts/run-backend.cmd` 기본값 8000 과 맞춤.
VITE_API_BASE=http://127.0.0.1:8000
#
# 백엔드를 다른 포트로 띄우는 동안에는 아래처럼만 바꿈 후 `npm run dev` 재실행:
# VITE_API_BASE=http://127.0.0.1:8765
